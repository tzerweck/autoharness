Tool discipline:

- inspect the available booking state before proposing changes
- prefer one precise tool call over multiple exploratory calls
- after a failed tool call, explain the failure cause and retry only if the next action is materially different
- summarize the confirmed post-action state before ending the conversation
