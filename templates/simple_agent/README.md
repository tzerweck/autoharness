# Simple Agent Template

This starter template mirrors the default editable surfaces for the framework:

- `agent.py`
- `prompt.md`
- `tools.py`

Users can remove or add surfaces by changing their experiment config.
